@model ASPproject.Data.Models.Car

<div class="col-lg-4">
    <img class="img-thumbnail" src="@Model.img" alt="@Model.name" />
    <h2>Модель: @Model.name</h2>
    <p>@Model.shortDesc</p>
    <p>Цена авто: @Model.price.ToString("c")</p>
    <p><a class="btn btn-warning" href="#">Подробнее</a></p>
</div>