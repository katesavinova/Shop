
    <h1>Все автомобили</h1><br>
    <h3>@Model.carCategory</h3>
    <div class="row mt-5 mb-2">
        @{

            foreach (ASPproject.Data.Models.Car car in Model.AllCars)
            {
                @Html.Partial("AllCars", car)
            }


        }
    </div>
